# On start loaded scripts

Scripts in this folder will be loaded on server start.

The are mostly used for testing purposes.

The script module should have the same name like file in which it is stored